import 'package:flutter/material.dart';

class ChannelFiles extends StatelessWidget {
  const ChannelFiles({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Channel Files'),
    );
  }
}