import 'package:flutter/material.dart';

class Channelchat extends StatelessWidget {
  const Channelchat({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Channel Chat'),
    );
  }
}